# ai-users-success.png

- 生成工具：ImageGen（built-in edit）
- 生成日期：2026-09-16
- 用例：`precise-object-edit`
- 画布：1536 × 1024
- 参考图片：`ai-users-detail.png`
- 上一状态：确认完成

## 保持不变

详情状态的画布、抽屉、表格、用户数据和布局。

## 本次变化

林夏变为已禁用，统计数字更新，主按钮变为启用用户，并显示成功 Toast。

## 完整提示词

```text
Use case: precise-object-edit
Asset type: final success state of an image-hotspot interactive prototype
Input image: Image 1 is the exact USER DETAIL state and edit target.
Primary request: Edit Image 1 to show that 林夏 has just been disabled successfully.
Required changes only: change the green "正常账号" badge in the right drawer to a coral-red "已禁用" badge; change the bottom-right red "禁用用户" button to a dark charcoal "启用用户" button; add a compact success toast at the lower right above the buttons with a green check icon and text verbatim "用户已禁用" and "林夏的访问状态已更新". In the dimmed table behind the drawer, change 林夏's status badge to "已禁用" and change the normal-account summary count from 3 to 2.
Constraints: preserve the exact 1536×1024 canvas, right drawer, sidebar, header, table, all other user data, layout, typography, colors and positions from Image 1. Do not add a modal. No prototype toolbar, no hotspot outlines, no watermark. Keep all text horizontal and crisp.
```
