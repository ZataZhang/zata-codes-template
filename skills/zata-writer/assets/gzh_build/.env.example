# 公众号 API 凭证（二选一）
#
# 推荐：直接配置系统环境变量，名字带 GZH_ 前缀，辨识度高、不怕误提交：
#   export GZH_APPID=你的AppID
#   export GZH_APPSECRET=你的AppSecret
# 写进 ~/.zshrc 后全局生效，gzh_build 复制到任何文章目录都能直接用。
#
# 备选：把下面两行的值填好，保存为同目录的 .env（.env 不进 git）。
# 脚本读取顺序：环境变量 GZH_APPID/GZH_APPSECRET → .env 的 APPID/APPSECRET。
APPID=
APPSECRET=
